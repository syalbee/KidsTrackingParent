package com.example.kidstrackingparent.dataClass

data class Childuserdata(
    var lastUpdate : String ?= null,
    var lat: Double ? = null ,
    var lon: Double ? = null ,
    var name : String ?= null,
    var photoUrl: String ? = null ,
    var position: String ? = null )
